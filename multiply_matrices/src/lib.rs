fn multiply_matrices(mat1: Vec<Vec<f64>>, mat2: Vec<Vec<f64>>) -> Vec<Vec<f64>> {
    unimplemented!()
}


#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn it_works() {
        let mat1 = vec![
            vec![1.0, 2.0],
            vec![3.0, 4.0]
        ];
        let mat2 = vec![
            vec![5.0, 6.0],
            vec![7.0, 8.0]
        ];

        // test
    }
}
